# Core Framework
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.7.4  # Versão mínima requerida por langchain 0.3.27 (>=2.7.4, <3.0.0)
pydantic-settings==2.12.0  # Compatível com pydantic 2.7.4 (corrige ERR-003) - versão mais recente estável
email-validator==2.3.0  # Necessário para EmailStr do Pydantic

# Banco de Dados
sqlalchemy==2.0.23
alembic==1.12.1
psycopg2-binary==2.9.9  # Driver PostgreSQL para SQLAlchemy
pgvector==0.2.4  # Para busca vetorial otimizada (PostgreSQL)

# Autenticação
python-jose[cryptography]==3.3.0
bcrypt==4.1.2  # Hash de senhas com bcrypt (usado diretamente)
python-multipart==0.0.6

# IA e Machine Learning
# sentence-transformers requer PyTorch e transformers como dependências
sentence-transformers==2.3.1  # Versão estável e compatível
torch==2.1.2  # PyTorch - será instalado automaticamente, mas fixamos versão
transformers==4.35.2  # Necessário para sentence-transformers
huggingface-hub>=0.16.4  # Requerido por transformers 4.35.2 (compatível com sentence-transformers 2.3.1 que requer >=0.15.1)

# Numéricas e Análise
numpy==1.26.2  # Compatível com torch e scikit-learn
pandas==2.1.4
scikit-learn==1.3.2

# LLM (GenAI)
groq==0.36.0  # Versão atualizada para suportar API de áudio (transcriptions)

# LangChain e RAG - Versões fixas e compatíveis entre si
# langchain 0.3.27 requer langchain-core>=0.3.72
# NOTA: langchain-classic não é necessário e causa conflitos - não instalar
langchain==0.3.27  # Core LangChain (chains, memory, etc)
langchain-core==0.3.72  # Core abstrações - versão mínima requerida por langchain 0.3.27
langchain-community==0.3.27  # Integrações comunitárias (PGVector, HuggingFaceEmbeddings)
langchain-groq==0.3.3  # Integração Groq - versão compatível com langchain-core 0.3.72
langchain-text-splitters==0.3.9  # Text splitters - versão compatível

# Áudio
edge-tts==6.1.9
pydub==0.25.1

# Utilitários
python-dotenv==1.0.0
httpx==0.25.1
requests==2.31.0  # Para scripts de teste
certifi==2023.7.22  # Versão fixa para compatibilidade com httpx

# Testes
pytest==7.4.3
pytest-asyncio==0.21.1

# Rate Limiting
slowapi==0.1.9

# NOTAS SOBRE DEPENDÊNCIAS:
# - langchain-classic não é necessário e causa conflitos com langchain 0.3.27
#   Se instalado acidentalmente, remover com: pip uninstall langchain-classic -y
# - edge-tts==6.1.9 (versão fixa para compatibilidade)
# - certifi==2023.7.22 (versão fixa para compatibilidade com httpx)